<%= form_with(model: pagecomment_page, local: true) do |form| %>
  <% if pagecomment_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pagecomment_page.errors.count, "error") %> prohibited this pagecomment_page from being saved:</h2>

      <ul>
      <% pagecomment_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pagecomment_id %>
    <%= form.number_field :pagecomment_id %>
  </div>

  <div class="field">
    <%= form.label :page_id %>
    <%= form.number_field :page_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
