<%= form_with(model: user_pagecomment, local: true) do |form| %>
  <% if user_pagecomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_pagecomment.errors.count, "error") %> prohibited this user_pagecomment from being saved:</h2>

      <ul>
      <% user_pagecomment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :pagecomment_id %>
    <%= form.number_field :pagecomment_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
